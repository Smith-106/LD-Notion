import{_ as a,o as e,c as n,a6 as i}from"./chunks/framework.wLlb4ebq.js";const k=JSON.parse('{"title":"Auth Model","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/auth-model.md","filePath":"concepts/auth-model.md"}'),s={name:"concepts/auth-model.md"};function d(o,t,l,r,h,c){return e(),n("div",null,[...t[0]||(t[0]=[i(`<h1 id="auth-model" tabindex="-1">Auth Model <a class="header-anchor" href="#auth-model" aria-label="Permalink to “Auth Model”">​</a></h1><p>Auth Model 解释 LD-Notion 如何获得 Notion 访问能力。OAuth 是推荐路径；manual token 是 advanced fallback，主要用于个人 Internal Integration、旧流程兼容和排障。</p><h2 id="_30-秒理解" tabindex="-1">30 秒理解 <a class="header-anchor" href="#_30-秒理解" aria-label="Permalink to “30 秒理解”">​</a></h2><ul><li>OAuth 是推荐路径：用户在 Notion 授权页批准访问，LD-Notion 保存 access token 与 refresh token。</li><li>manual token 是高级兜底：用户手动复制 <code>secret_</code> Integration Token 到面板。</li><li>两种方式都运行在纯前端环境；敏感凭证会持久化到本地加密保险箱，解锁后仅在当前会话中可直接使用。</li><li>断开授权只清除本地凭据，不会撤销 Notion 后台已经批准的授权。</li><li>Token 可用不等于目标可写；目标数据库或页面还必须连接对应 Integration。</li></ul><h2 id="oauth-manual-token-matrix" tabindex="-1">OAuth / manual token matrix <a class="header-anchor" href="#oauth-manual-token-matrix" aria-label="Permalink to “OAuth / manual token matrix”">​</a></h2><table tabindex="0"><thead><tr><th>Dimension</th><th>OAuth</th><th>manual token</th></tr></thead><tbody><tr><td>Recommended status</td><td>推荐路径，适合日常使用。</td><td>advanced fallback，适合个人集成、调试和 OAuth 不可用时使用。</td></tr><tr><td>Setup</td><td>配置 Client ID、Client Secret、Redirect URI 后点击一键授权。</td><td>在 Notion 创建 Internal Integration，复制 <code>secret_</code> token。</td></tr><tr><td>Stored locally</td><td><code>Client ID</code>、<code>Redirect URI</code>、workspace meta 保存在本地配置；access token、refresh token、<code>Client Secret</code> 保存在本地加密保险箱。</td><td>Integration token 保存在本地加密保险箱。</td></tr><tr><td>Refresh</td><td>access token 可通过 refresh token 续签。</td><td>不支持自动 refresh；失效后需要重新复制。</td></tr><tr><td>User effort</td><td>初次配置稍多，后续较少。</td><td>每个用户都需要理解 Integration 与 Connections。</td></tr><tr><td>Security note</td><td><code>Client Secret</code> 会进入本地加密保险箱，但项目仍是纯前端，不适合共享生产级 secret。</td><td>token 本身就是长期密钥；虽然现在也保存在本地加密保险箱中，泄露后仍应在 Notion 后台轮换。</td></tr><tr><td>Best fit</td><td>个人自建公开集成、一键授权体验、减少手动 token 粘贴。</td><td>本地个人使用、OAuth 配置失败、排查 Notion API 访问问题。</td></tr><tr><td>Failure fallback</td><td>重新授权，或临时切换到 manual token。</td><td>检查 token、Capabilities、Connections，或改用 OAuth。</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">本地凭据风险</p><p>LD-Notion 没有独立后端。OAuth Client Secret、OAuth token、manual token、AI API Key 等敏感凭证现在会优先保存在本地加密保险箱中，而不是继续长期留在旧明文键里；但它们依然属于前端本地持有的密钥材料。该模式适合个人自用，不适合把共享生产级 secret 放进前端配置。</p></div><h2 id="oauth-flow" tabindex="-1">OAuth flow <a class="header-anchor" href="#oauth-flow" aria-label="Permalink to “OAuth flow”">​</a></h2><div class="language-mermaid"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant User as 用户</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Panel as LD-Notion 面板</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant NotionOAuth as Notion OAuth</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Store as 本地加密保险箱 + 配置存储</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant Guard as OperationGuard</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  participant API as Notion API</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  User-&gt;&gt;Panel: 填写 OAuth 配置并点击一键授权</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Panel-&gt;&gt;NotionOAuth: 打开授权 URL</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  NotionOAuth--&gt;&gt;Panel: 返回 code/state</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Panel-&gt;&gt;NotionOAuth: 交换 access token / refresh token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Panel-&gt;&gt;Store: 保存本地凭据与 OAuth 配置</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  User-&gt;&gt;Panel: 发起读取或写入</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Panel-&gt;&gt;Guard: 提交 auth state + operation</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Guard-&gt;&gt;API: 允许后使用 access token 调用</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  API--&gt;&gt;Panel: 返回 workspace / page / database 结果</span></span></code></pre></div><h2 id="auth-routing" tabindex="-1">Auth routing <a class="header-anchor" href="#auth-routing" aria-label="Permalink to “Auth routing”">​</a></h2><table tabindex="0"><thead><tr><th>Priority</th><th>Condition</th><th>Route</th><th>Stop condition</th><th>User-visible result</th></tr></thead><tbody><tr><td>1</td><td>OAuth connected and access token valid</td><td>使用 OAuth token。</td><td>目标未连接 Integration。</td><td>显示工作区、数据库或页面列表。</td></tr><tr><td>2</td><td>OAuth access token expired and refresh token exists</td><td>尝试刷新后重试。</td><td>refresh 失败或 state 不一致。</td><td>提示重新授权。</td></tr><tr><td>3</td><td>manual token exists</td><td>使用 manual token。</td><td>token 格式错误、401、403 或目标不可达。</td><td>提示检查 token 和 Connections。</td></tr><tr><td>4</td><td>no credential</td><td>阻止远端写入。</td><td>无。</td><td>打开授权配置入口并保留预览。</td></tr></tbody></table><h2 id="target-access-contract" tabindex="-1">Target access contract <a class="header-anchor" href="#target-access-contract" aria-label="Permalink to “Target access contract”">​</a></h2><p>Notion 授权只说明 token 有机会访问 workspace，不保证目标已经开放给 Integration。写入前仍需检查：</p><ol><li>Integration 是否具备 <code>Read content</code>、<code>Update content</code>、<code>Insert content</code>。</li><li>目标 database 或 page 是否在 Notion 的 <code>Connections</code> 中连接该 Integration。</li><li>用户选择的是 database 模式还是 page 模式。</li><li>手动输入 ID 时是否只输入 32 位 ID，而不是完整 URL。</li></ol><h2 id="failure-modes" tabindex="-1">Failure modes <a class="header-anchor" href="#failure-modes" aria-label="Permalink to “Failure modes”">​</a></h2><table tabindex="0"><thead><tr><th>Failure</th><th>Likely cause</th><th>Fix</th></tr></thead><tbody><tr><td>OAuth callback failed</td><td>Redirect URI 不一致、state 过期或配置缺失。</td><td>对齐 Notion 后台与面板中的 Redirect URI。</td></tr><tr><td>Workspace list empty</td><td>Integration 未连接任何目标。</td><td>在 Notion 页面或数据库的 <code>Connections</code> 中添加 Integration。</td></tr><tr><td><code>401</code></td><td>token 过期、错误、撤销或被手动覆盖。</td><td>OAuth 重新授权，或更新 manual token。</td></tr><tr><td><code>403</code></td><td>Integration 没有目标权限。</td><td>检查 Capabilities 与目标 Connections。</td></tr><tr><td>Disconnect 后 Notion 后台仍显示授权</td><td>本地清除不等于后台撤销。</td><td>到 Notion Integration 后台撤销授权。</td></tr></tbody></table><h2 id="contract" tabindex="-1">Contract <a class="header-anchor" href="#contract" aria-label="Permalink to “Contract”">​</a></h2><ul><li>OAuth 是推荐路径。</li><li>manual token 是 advanced fallback。</li><li>Auth failure 必须在 OperationGuard 或目标 writer 前阻止写入。</li><li>审计日志和示例不得包含真实 token、Client Secret 或 API Key。</li></ul>`,18)])])}const u=a(s,[["render",d]]);export{k as __pageData,u as default};
