{
  "name": "ld-notion-hub",
  "version": "3.14.8",
  "private": true,
  "description": "LD-Notion Hub — AI 多源知识中枢",
  "main": "LinuxDo-Bookmarks-to-Notion.user.js",
  "scripts": {
    "test": "npx vitest run && npm run test:legacy",
    "test:legacy": "node tests/utils.test.js && node tests/logic-modules.test.js && node tests/notion-oauth.test.js",
    "verify:baseline": "node scripts/verify-baseline.js",
    "build:extension": "node scripts/build-extension.js --src dist/LinuxDo-Bookmarks-to-Notion.user.js",
    "verify:extension:bounded": "node -e \"const fs=require('fs'); const os=require('os'); const path=require('path'); const { buildExtension } = require('./scripts/build-extension.js'); const outDir=fs.mkdtempSync(path.join(os.tmpdir(),'ld-notion-build-bounded-')); try { buildExtension({ manifestProfile: 'bounded_hosts', outDir }); } finally { fs.rmSync(outDir, { recursive: true, force: true }); }\"",
    "verify:bridge-extension": "node scripts/verify-bridge-extension.js",
    "verify:extension:surfaces": "node scripts/verify-extension-surfaces.js",
    "verify:equivalence": "node scripts/verify-bundle-equivalence.js",
    "verify:delivery": "node scripts/verify-delivery.js",
    "build": "node build.js",
    "verify:build": "node scripts/verify-build.js",
    "docs:dev": "vitepress docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "verify:build:negative": "node scripts/verify-build.js --negative-check"
  },
  "keywords": [],
  "author": "Smith-106",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitepress": "^2.0.0-alpha.17",
    "vitest": "^4.1.8"
  }
}
