# UI 手工回归清单（Linux.do / Notion / 通用网页 / chrome-extension-full）

> 适用脚本：`LinuxDo-Bookmarks-to-Notion.user.js`
>
> 建议每次 UI 大改后先跑自动化基线：`npm run verify:baseline`。如涉及扩展形态，再补 `npm run verify:extension:bounded` 与 `npm run build:extension`，然后按本清单做手工回归。

## 0. 通用检查（所有站点）

- [ ] 页面刷新后 UI 样式不重复注入（无明显样式叠加/抖动/字体异常）
- [ ] 按钮/输入/选择器在 `Tab` 导航下有清晰焦点环（`:focus-visible`）
- [ ] 系统开启“减少动态效果”（`prefers-reduced-motion`）时，无明显闪烁/强动画

## 1. Linux.do（收藏页 UI 面板）

打开：`https://linux.do/u/<你的用户名>/activity/bookmarks`

- [ ] 面板出现、标题栏可拖拽移动；缩放手柄可用（左边/下边/左下角）
- [ ] “加载收藏列表”可用，计数更新，列表渲染正常
- [ ] 勾选/全选/取消全选正常，已选数量显示正确
- [ ] 导出流程按钮状态正确（开始/暂停/继续/取消），进度条与状态提示显示正常
- [ ] AI 对话区可发送、可清空；设置折叠可打开/关闭；输入框回车发送正常
- [ ] 日志面板可展开/收起，条目渲染正常；“清除日志”生效

## 2. Notion（浮动 AI 面板）

打开任意页面：`https://www.notion.so/`

- [ ] 右下角浮动按钮出现；可拖拽；点击可开合面板
- [ ] 面板可拉伸（左边/上边/左上角），关闭/展开逻辑正常
- [ ] 对话区可发送；快捷 chips 点击可触发发送
- [ ] “设置”折叠区可开合；保存设置后刷新仍能加载配置
- [ ] 状态提示（info/success/error）显示与关闭按钮正常

## 3. 通用网页（剪藏导出面板）

打开任意非 Linux.do/Notion 页面（例如新闻/博客/文档页）

- [ ] 右下角剪藏按钮出现；点击可打开/关闭面板
- [ ] 面板预览区域显示标题/来源等元信息
- [ ] 设置 API Key 的流程正常（Key 不预填到 DOM）
- [ ] 导出按钮可触发导出；exporting/success/error 三种状态视觉可区分且可读
- [ ] 面板可拉伸（左边/上边/左上角），不会遮挡主要操作按钮

## 4. `chrome-extension-full`（独立扩展形态）

准备：

- [ ] 先执行 `node scripts/build-extension.js`
- [ ] 在 `chrome://extensions/` 中以“加载已解压的扩展程序”方式加载 `chrome-extension-full/`

检查：

- [ ] 扩展 Popup 可以正常打开，不出现空白页或脚本报错
- [ ] Popup 中的快捷入口文案与当前稳定能力一致，不宣传内部或不稳定能力
- [ ] 点击 Popup 中的来源/导入入口后，能把当前页面引导到对应宿主面或触发对应消息桥接
- [ ] 在 Linux.do / Notion / GitHub / Zhihu / 任意通用网页上，content script 注入后不会出现明显的重复 UI、缺失样式或初始化失败
- [ ] GitHub 页面能进入完整面板；Zhihu 与任意通用网页能出现右下角剪藏入口
- [ ] 重新执行 `node scripts/build-extension.js` 后，扩展目录结构仍可被浏览器正常加载

## 6. v3.14.0 新增行为回归（UI 可控制能力审计 30 项）

- [ ] 各来源（Linux.do/GitHub/书签/RSS）「立即导入」按钮独立可用，点击有状态反馈
- [ ] 同步中心三链状态（书签/多源/RSS 自动导入）显示「尚未同步」或实际状态，不空白（SyncState facade 委托修复）
- [ ] 主题切换三态（浅色/深色/跟随系统 auto）可选并持久化
- [ ] filter/ai-settings 等折叠区折叠后重新导航仍保持 collapsed
- [ ] 设置面板「面板尺寸重置」将自定义宽度恢复默认
- [ ] Obsidian 配置区有「测试连接」按钮，点击返回明确结果
- [ ] AI 处理中发送指令提示「AI 正在处理上一条指令,请稍候再试」
- [ ] 导出目标摘要行显示「导出目标:数据库 ××× · 权限:标准 · 授权:Manual/OAuth」
- [ ] 视图页「保存到 Notion」按钮点击后进入 loading 态且不可重复点击
- [ ] 关闭面板弹出确认对话框（5 秒倒计时）,确认后面板销毁,文案为「关闭后可通过刷新页面重新打开」
- [ ] 反馈类结果（配置验证成功/失败）在就地位置与全局状态容器双写
- [ ] 权限指示器显示当前权限级别；审计开关切换记录 audit.enabled/disabled 事件

## 7. v3.11.0 新增行为回归（Odyssey UI）

- [ ] Linux.do 普通帖页面仅一个浮动入口（无 📎 通用浮钮）；主面板最小化后仅显示 📚
- [ ] 已导出条目点「重新导出」弹出确认框：取消不改变状态，确认后条目回到待导出列表
- [ ] 主面板可见时按 Esc 收起为 mini 按钮；焦点在输入框、IME 组合中或确认弹窗打开时按 Esc 不收起
- [ ] ≤480px 视口：三套面板变底部抽屉、浮钮缩小贴边；481–768px：浮钮 48px
- [ ] 触屏（或 DevTools 触摸模拟）：标题栏拖动面板、浮动按钮拖拽换位、边缘手柄拉伸均可用（pointer events + touch-action）
- [ ] gclip 面板关闭后 300ms 内快速重开，面板保持可见不消失（关闭过渡竞态）
- [ ] gclip 面板内容超高时内部滚动，整体不溢出视口（max-height 钳制）
